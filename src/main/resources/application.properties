spring.application.name=Araksha-smart-disaster-relief-Backend
spring.datasource.url=jdbc:mysql://localhost:3306/arakshadb?createDatabaseIfNotExist=true
spring.datasource.username=root

spring.datasource.password=eshan#12


spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Multipart File Upload Settings
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# Custom Upload Configuration
file.upload-dir=./uploads

# ========== Gmail SMTP Configuration for OTP Email ==========
# IMPORTANT: Replace with your Gmail address and Google App Password
# To generate App Password: Google Account > Security > 2-Step Verification > App Passwords
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=esesh47@gmail.com  
spring.mail.password=ahzbisrytryaotfn   
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# ========== External API keys (Google Maps, OpenWeatherMap, Twilio) ==========
google.maps.api.key=${GOOGLE_MAPS_API_KEY:mock}
openweathermap.api.key=${OPENWEATHERMAP_API_KEY:mock}
twilio.account.sid=${TWILIO_ACCOUNT_SID:mock}
twilio.auth.token=${TWILIO_AUTH_TOKEN:mock}
twilio.phone.number=${TWILIO_PHONE_NUMBER:mock}
araksha.admin.email=${ARAKSHA_ADMIN_EMAIL:araksha-admin@example.com}

